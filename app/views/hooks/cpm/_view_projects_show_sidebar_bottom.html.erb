<% if (!Setting.plugin_redmine_cpm['ignored_projects'].present? or !Setting.plugin_redmine_cpm['ignored_projects'].include?(@project.id.to_s)) and User.current.allowed_to? :manage_members, @project -%>
    <%= link_to l(:"cpm.label_allocation"), {:controller => 'cpm_management', :action => 'show', 'projects[]' => @project.id, 'time_unit' => 'month', 'commit' => l(:"cpm.button_find")} %>
  </p>
<% end %>
